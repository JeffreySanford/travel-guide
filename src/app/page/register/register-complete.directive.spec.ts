import { RegisterCompleteDirective } from './register-complete.directive';

describe('RegisterCompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterCompleteDirective();
    expect(directive).toBeTruthy();
  });
});
